<%= link_to "ユーザー登録", new_user_registration_path(),class: "my-1 btn btn-outline-primary"%><br />
<% if current_user.admin? %>

<u1><% @users.each do |user| %>
  <li>
  <%= user.id %><br>
  <%= user.role %>
  <%= link_to '編集', edit_user_path(user.id), class: "btn btn-outline-info my-1 mp-1" %><br>

    <% unless user.ticket_watchers.empty?%>
    <p>ウォッチャーを指定</p>
      <% @tickets.each do |ticket| %>
      <%= ticket.category %><br>
      <%= ticket.status %>
      <% end %>
    <% end %>
</li>
  <% end %>

<% else %><br>

<p>テスト表示※後で削除</p>
<p>最終的にユーザー名のみ表示</p>
<u1><% @users.each do |user| %>
  <li>
  <%= user.id %><br>
  <%= user.role %>
  <%= link_to '編集', edit_user_path(user.id), class: "btn btn-outline-info my-1 mp-1" %><br>

    <% unless user.ticket_watchers.empty?%>
    <p>ウォッチャーを指定</p>
      <% @tickets.each do |ticket| %>
      <%= ticket.category %><br>
      <%= ticket.status %>
      <% end %>
    <% end %>
</li>
  <% end %>
  <% end %>
